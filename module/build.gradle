allprojects
{
    group = MAVENGROUP

    apply plugin: 'maven'

    uploadArchives {
        repositories {
            mavenDeployer {
                repository(url: MAVENLOCATION)
            }
        }
    }
}

